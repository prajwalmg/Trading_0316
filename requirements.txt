# ============================================================
#  requirements.txt — TradingFirm OS
#  Python 3.11 (recommended)
#  Install: pip install -r requirements.txt
# ============================================================

# ── Data ─────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0          # Parquet cache
yfinance>=0.2.28         # Fallback + commodities (no free alternative)

# ── Data sources (new) ───────────────────────────────────────
# requests is already listed under Execution — used by OANDA,
# Binance, and FRED fetchers (no extra packages needed for those)
#
# OANDA:   uses requests — free practice account at oanda.com
# Binance: uses requests — no account needed
# FRED:    uses requests — free key at fred.stlouisfed.org
#
# Optional: imbalanced-learn for SMOTE class balancing
imbalanced-learn>=0.11.0

# ── ML Models ────────────────────────────────────────────────
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.1.0          # Python 3.11 only

# ── Execution & broker APIs ──────────────────────────────────
requests>=2.31.0
alpaca-trade-api>=3.0.0  # Alpaca broker + Alpaca historical data

# ── Utilities ────────────────────────────────────────────────
python-dateutil>=2.8.0
pytz>=2023.3
tqdm>=4.66.0

# ── Portfolio optimisation ────────────────────────────────────
cvxpy>=1.4.0               # Mean-variance portfolio optimisation (Prompt 6)

# ── Deep learning (optional — LSTM 5th base model) ───────────
# torch>=2.1.0             # PyTorch — install separately for your platform:
#   CPU:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   CUDA: pip install torch torchvision (auto-selects GPU build)
# If not installed, LSTM falls back gracefully to neutral probabilities.

# ── Alerts ───────────────────────────────────────────────────
# Telegram: no extra package needed — uses stdlib urllib.request
# Set env vars: TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID

# ── Install order ─────────────────────────────────────────────
# pip install --upgrade pip
# pip install pandas numpy pyarrow yfinance
# pip install scikit-learn xgboost lightgbm imbalanced-learn
# pip install requests alpaca-trade-api
# pip install python-dateutil pytz tqdm
# pip install cvxpy
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
